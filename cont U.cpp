#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,x;
    cin>>n>>x;

    vector<int>v(n);

    for(auto& a : v)
        cin>>a;

    vector<int>v2(n);

    for(int i=0;i<n;i++)
    {
        v2[i]=v[i];
    }

    for(int i=0;i<n;i++)
    {
        for(int j=0;j<n;j++)
        {
            if
        }
    }
}
